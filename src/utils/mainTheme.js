import styled from "styled-components/native";

const StyledButton = styled.Button`
  background-color: black;
  font-size: 32px;
  color: white;
`;

const Title = styled.Text`
  font-size: 15px;
  color: red;
`;

export default (StyledButton, Title);
